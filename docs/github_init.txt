#this is a document of how to init github and doing other changes
git init
git add .
git commit -m ''

git remote add origin <remote repository URL>

git remote -v
#verifies the new remote URL

git push origin master
